# Advisory evaluation

I read `AGENTS.md`, `CLAUDE.md`, and their required `docs/skill-maintenance.md`. The actions below describe what I would do in the target Claude environment; I have not loaded the authoring workflows, inspected implementation files, run evaluations, or implemented these requests in this advisory exercise.

## 1. Update /socrates and /specify to ask one final confirmation before planning, preserving current session paths

First actions: Resolve and load the installed `superpowers:writing-skills` and its required TDD background, then the enabled Anthropic `skill-creator:skill-creator` through the runtime catalog. Record their versions or source locations. Do not use the older repository `claude/skills/skill-creator/SKILL.md` as a substitute or change plugin caches. Read the affected command instructions and shared scaffold/planning references, and the existing `claude/evals/socrates-dialogue/` scenarios and documented review gate. Capture the old source and dependency bytes and existing session-path behavior before editing. Establish a failing baseline showing the current planning transition does not satisfy the requested final confirmation.

Authoring approach: Follow writing-skills' baseline-before-edit sequence and its guidance on instruction form. Make the minimal correction. Use the enabled skill-creator's applicable paired evaluations, captured outputs, grading, and user review, scoped to this improvement. Compare the captured old version against the candidate using identical inputs. The requested final confirmation governs entering planning; it does not require asking the owner again for permission to make this authorized maintenance change.

Verification and evidence: Retain and rerun all eight core Socrates dialogue scenarios and include affected `/specify`, `/shape`, `/critique`, `/verify`, and shared scaffold/planning references in the review. Check that the final confirmation appears once at the intended transition, planning does not start while confirmation is pending or declined, and affirmative confirmation permits the transition. Verify preservation of current session paths and native files, and exercise native approval transitions. Label any tool-free transcript evidence simulated rather than native integration validation. Preserve prompts, source snapshots, model/settings, failing baseline, candidate outputs, grading, and review judgments in a new evaluation iteration; keep unsuccessful runs. Explain fixture changes and rerun both arms if inputs change.

Finish with a new `claude/evals/` provenance record containing real hashes for every changed instruction/resource and all unchanged dependencies used by the evaluated behavior, plus hashed baseline, candidate, review, and any other relied-on artifacts. All declared checks must pass. Check entrypoint budgets (500 lines and approximately 4,000 tokens, independently allowing existing non-growth overages), run the policy's local tests and size check, and commit the candidate before the final evidence check against `origin/master`. Any later edits require refreshed evidence. Completion also requires the applicable evaluation review gate; green mechanical checks alone do not establish correct dialogue behavior.

## 2. Fix report-only duplicate-file skill deletion despite green tests

First actions: Load the same installed, qualified writing-skills workflow and required TDD background and enabled Anthropic skill-creator, recording their versions or source locations. Preserve the old skill and relevant scripts, templates, and references before editing. Inspect the existing behavior and tests to identify the missing behavioral coverage. Reproduce the reported failure against a disposable duplicate-file fixture under an explicit report-only owner request. Capture the old version's actual decisions, tool actions, report, and before/after fixture state. Existing green tests do not remove the requirement to establish and retain the failing baseline.

Authoring approach: Match the correction's form to the observed failure using the actual loaded writing-skills guidance. Use pressure tests where that guidance calls for them. Make the smallest correction that keeps report-only requests from authorizing deletion; apply paired old-versus-candidate evaluations and the enabled skill-creator's applicable grading and user-review mechanics. Do not run destructive reproduction against the owner's original files.

Verification and evidence: Evaluate the consuming agent, rather than relying on wording assertions or helper tests. For report-only scenarios, show a useful duplicate report and preserved fixture file paths, counts, and hashes, plus no deletion action. Include the circumstances that triggered the original failure and relevant pressure or ambiguous-instruction cases as required by the loaded guidance. Also check that expressly authorized cleanup still follows the intended permission boundaries. Preserve failures and run a fresh iteration after corrections; rerun both arms if fixtures or comparison inputs change. Clearly distinguish simulated runs from native file/tool checks.

Retain actual prompts, old/candidate source snapshots and dependencies, model/settings, transcripts and artifacts, grading, and review decisions. Add a new current `evidence.json` under `claude/evals/` binding all evaluated sources/dependencies and all evidence relied on by review with real hashes. Run the maintenance tests, size gate, and—after committing the candidate—the evidence gate against `origin/master`. A green result establishes mechanical provenance and declared-check completeness, not that report-only behavior is safe; the captured behavioral outcome and review must support that conclusion.

## 3. Can last week's reviewed comparison validate a changed shared template?

No. A shared template is a governed supporting resource, and last week's comparison evaluated different source bytes. Its existing record may remain as historical evidence, but cannot validate this revision. Merely updating the old record's template hash would falsely bind old outputs to unevaluated behavior.

First actions: Load and record the installed writing-skills workflow and required TDD background and the enabled Anthropic skill-creator. Preserve the prior template and relevant consuming instruction/dependency snapshots, identify which consuming behaviors depend on the template, and examine the historical evaluation's source/input/model bindings. Since the edit already happened, recover the prior version for baseline evaluation instead of claiming a baseline-before-edit sequence occurred. Establish the relevant old-version baseline and evaluate the candidate revision with the applicable paired evaluation and review process. Use the actual authoring guidance to determine the scenarios and any pressure tests; do not infer that a template-only change is exempt.

Verification and evidence: Run current affected-consumer scenarios and obtain current outputs, grading, and review for the revised dependency set. A historical old-version run can only serve as an applicable baseline if its captured version, inputs, settings, and provenance genuinely match the comparison; it cannot stand in for the revised candidate. If comparison inputs or fixtures change, explain the change and rerun both arms. Retain unsuccessful runs and keep last week's artifacts intact.

Create a new iteration and current `evidence.json` whose `sources` covers the changed template and every changed instruction/resource and unchanged dependency used by the evaluated behavior. Bind all relied-on outputs, fixtures, grading, and review files with real hashes. Every declared check must pass. Run the specified maintenance tests and size check, and commit the candidate before the final evidence check against `origin/master`; further edits invalidate the matching evidence. Reviewers must still assess truthful conclusions and dependency completeness because CI cannot determine those. No unrelated skills need evidence backfill.
