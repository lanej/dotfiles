---
name: arc
description: "Arcanist CLI for Phabricator. Use phab instead — it covers all Phabricator operations without requiring arc directly."
---

# arc → use phab instead

Do not use `arc` commands directly. Use `phab` for all Phabricator operations, including raw Conduit API calls via `phab api <method> [params-json]`.
