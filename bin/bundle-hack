#!/usr/bin/env bash

if [[ ! -s $(which bundle 2>/dev/null) ]]; then
  gem install bundler --no-document -qq
fi

(bundle check 1>/dev/null || bundle install --local --jobs 5 || bundle install --jobs 5) && bundle "$@"
