# Lancer database (can be regenerated)
.lancedb/

# Jupyter cache (Quarto execution cache)
.jupyter_cache/

# Data snapshots (can be regenerated from scripts)
data/snapshots/

# OS files
.DS_Store

# Editor files
.vscode/
.idea/
*.swp

# Playwright MCP artifacts (screenshots/snapshots, not project content)
.playwright-mcp/

# MCP Memory (personal context, not shared)
memory.json
.memory.json
.claude/

**/.claude/settings.local.json

**/.claude/.cc-writes/
